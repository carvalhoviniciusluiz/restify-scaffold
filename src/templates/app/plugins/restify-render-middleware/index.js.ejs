import path from 'path'

export const restifyRenderMiddleware = require('restify-render-middleware')({
  engine: {
    name: 'pug',
    extname: 'pug'
  },
  dir: path.join(__dirname, '../../views')
})
