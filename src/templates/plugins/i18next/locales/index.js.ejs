import { default as ptBR } from './pt-BR'

// main language
export const lng = 'pt-BR'

// supported languages
export const fallbackLng = ['pt-BR']

export default {
  'pt-BR': {
    translation: ptBR
  }
}
